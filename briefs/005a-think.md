# Brief 005a — the think act (CHARTER v0.1): a computation the one `decide` can buy

**Done when** the `cage` check is green on a pull request from `b/005a-think`. Kit v0.7 adds `kit_think.py` and its reference `meta_check.py`; the law gained a second signed page, `charter/CHARTER-v0.1.md` (tag `charter-v0.1`). Read the new page fully first, then `INTERFACE.md`'s kit v0.7 section, then both kit headers.

**The owner's numbers for this brief** (you choose none): none. This brief declares no pack. Every f, ops, r the kit uses is in the kit's own Worlds; the packs come in brief 005b, after SURFACE v0.1 is signed.

1. **`decide⁺`, beside `decide`, in `src/wald/decide.py`.** The page's §2 `think` block, written once. It reads `V_d(b,M)`, `cap(b,M)`, `M`, `s(b)`, `n` and the declared tables, and nothing else (S9); when θ is bought it evaluates `decide_min(d⁺,n)` — your existing lookahead with a different `n`, not a second copy of it (E5) — and returns that act. It returns the S7 bucket alongside the act and the predicted cost, in the five names INTERFACE gives. The cap is one pass over the live states and the menu (`_dot` of the measure against `best(ω)`), no lookahead: it must not call `_value` at any `n > 0`. Verify by hand against Appendix A and B of the page before running anything.
2. **The episode loop, one line changed.** `episode.run`'s `act = decide(belief, world, min(world.d, n), used)` becomes the decide⁺ step. θ is not an act the door sees: when the step reports `"think"`, `paid` goes to `Result.thought`, the bought act is played as if `decide` had minted it, and `n` is not decremented. `Result` gains `thought`, `steps` and `operations` (INTERFACE). Nothing else in the loop moves: the page's order after the door mints `o` is unchanged.
3. **`world.declare` and the new nouns.** The spec gains `dplus`, `fraction`, `rate`, `ops`, `table_sources.fraction/cost/rate`, optional `score`. Refuse by name: `FRACTION`, `COST`, `DEPTH_PLUS`, `RATE`, `UNSCORED`, with the exact conditions in INTERFACE. A spec without `dplus` is a v0 World and must play exactly as before: `kit.py`'s v0 suites and the wordle suites must stay green, and `step` must report `"floor"` on it.
4. **The operation counter (E6).** Count one per arithmetic operation on ℚ in `belief._dot` and `belief._split` (and any other place a fast path multiplies or adds measures). Reset at the start of executing θ, read at its end, appended to `Result.operations`. Read by nothing in `decide`: the lint will look. Print it in `report` beside the predicted `ops(s(b))`.
5. **`kit_adapter.make_agent()` gains `step`.** A thin call into decide⁺; converts and returns `(act, how, paid)`. It chooses nothing.
6. **`KERNEL.md`**: one line for the new function and for the counter. **`tests/`**: Appendix A and B of the page as tests, worked from the page's numbers, and the `FIXED_CAP` World (in `meta_check.fixed_worlds()`): your cap must be ≥ V₅ = 40951/2000 there.
7. **Nothing approximate, nothing Wordle.** The cap, the gain bound and the cost are exact rationals; no float, no threshold, no clock. `time` stays out of `src/wald`.

Where the page and `meta_check.py` disagree, the page decides and the kit is the usual culprit: `QUESTIONS.md`, with the World. Where the page admits two readings, the same, and stop on that point.

Measure and report in the PR: the cage command's time before and after; how many steps of the kit's Worlds fell in each S7 bucket (the kit prints the reference's counts; yours should match); the realised operation count of a think act on Appendix B against the predicted 200.
